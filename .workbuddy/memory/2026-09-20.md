# 2026-09-20

## rh-short-drama 技能：新增 R6a（无台词镜时长豁免）

用户口径：**特殊的无台词分镜不必遵循「无台词段压到 4-8 秒」**，典型两类是
① 需要铺垫的场面；② 特殊爆款长镜头。

落地为红线 **R6a**，并把豁免链到各处口径：

- `SKILL.md` 新增 R6a 行（限两类 + 15 秒硬上限 + 显式标注 + 每集 ≤2 个/≤20% + 只放松时长）
- `SKILL.md` 第 1 步第 7 条、R6 行、R7b 行加豁免指引；常见坑速查加一行
- `references/rhythm.md`：硬性指标表、§四新增「例外：铺垫镜与长镜头」小节、
  竖屏/横屏两张节奏表、每集自检清单

**标注约定**：分镜表里写 `[豁免:铺垫]` / `[豁免:长镜]`，不标注即按 4-8 秒卡。
**未豁免的**：R5 的 15 秒大分镜硬上限（服务端技术限制）、「不允许纯过场」、「前 3 秒必须入戏」、
R7a 台词占比 ≥55%。

## rh-short-drama 技能：新增 `styles/` 预设风格库

用户要求：**新增专门存放预设风格的文件夹；工程启动时询问用户选择风格，从文件夹里已有的风格挑；
每种风格一个独立 md**。首个风格 = 国漫 3D CG。

- 新建 `styles/guoman-3dcg.md`，含 7 段结构：元数据（frontmatter 给 `style_key` /
  `asset_tag` / `ref_subdir` / `char_field`）→ 一句话定位 → **风格特征八维**（逐维给
  要什么/不要什么，并标注落到「视觉风格」段哪一维）→ **中英双锁定串** →
  NEG → 互斥项 → 落地清单 → 验收清单
- 关键设计：风格文件必须给**中文锁定串（图像用）+ 英文风格层（视频用，R11）**，
  整串照抄；只写散文描述等于放任风格漂移
- `SKILL.md` 新增「🎨 风格预设（`styles/`）」章节：开工询问流程、已有预设表、
  必须给全的五样、新建模板（四反引号包裹，避免污染）、换风格连带影响表；§0 第 3 问答改为"列目录让用户挑"
- 顺带修一处既有矛盾：`character-consistency.md` 的「画风基准」改为**简版摘要**并指向风格文件
  （权威口径归 `styles/`），并显式说明「眼睛略大于真人」与 `NO enlarged eyes` 并不冲突
- 资产命名与风格文件对齐：`CH##_四视图_<风格标记>.png`（`guoman-3dcg` → `3DCG`），
  SKILL 第 3 步表、`naming.md`、`character-consistency.md` 三处同步

## 用户纠正：**「这是两种风格，不是两个版本」**

我原先把 `_CG版`（古风CG）当成 3DCG 的"旧命名/兼容写法"来处理，方向错了。
用户明确：**古风CG 与 国漫3DCG 是两种并列的风格**，各是一个独立预设，不是同一画风的两个版本。

**已改（把「版本」口径全部换成「风格」口径）**：

- `SKILL.md`「风格预设」节新增 **风格 vs 版本** 对照表；已有预设表登记古风CG 为「待建」并给资产标记 `古风CG`
- `SKILL.md`「多风格并存」节：每套画风 = `styles/` 一个风格文件，并列而非版本
- `naming.md`：角色四视图注加「风格标记 ≠ 版本号」；「版本后缀规则」节加范围限定（只管同风格内的重生成）
- `styles/guoman-3dcg.md`：删掉"命名兼容"那段，换成「风格标记不是版本号」
- 措辞统一：「角色图按风格分**文件**（`file` / `file_ref3d`）」，不再说"分版本"
- 顺带对齐既有约定：`ref_subdir: ref3d`（原写成 3dcg）、`char_field: file_ref3d`；角色生成器标注为「一风格一脚本」

**未做**：古风CG 风格文件**没建**。技能里关于它的材料只有"按通用提示词文本归纳"与"成图呈厚涂感"
两条，不足以写出可信的锁定串；**不臆造**，等用户给特征。

## rh-short-drama 技能：新增「传播层」爆款改编能力

用户要求：让技能具备爆款改编能力（选题拆解、结构模板、情绪钩子、标题与开头优化、平台调性适配），
并给可落地的功能设计与实现思路。

**设计定调**：现有链路是**制作层**（做得出来），新增一层**传播层**（有人看），
**叠加不覆盖**（不得推翻 R5/R6a/R7a）。

- 新建 `references/viral.md`：七个能力模块
  ① 选题体检（六项打分 ≥8/12，可视化冲突/台词密度任一 0 分否决）
  ② 母题骨架 5 个 + 90 秒节拍表（0-3 钩子 / 3-10 立矛盾 / ~30 首次反转 / 每 20-30s 节点 / 末 5-10 强钩）
  ③ 情绪钩子（开场钩三类 / 结尾钩三类 / 节点间隔）
  ④ 标题公式槽位 + 封面（复用关键帧 S01，不额外出图）
  ⑤ `platforms/` 机制（与 `styles/` 完全对称）
  ⑥ 合规一票否决全表 + **AI 标注三处**
  ⑦ **数据归因表**（现象 → 真因模块 → 先改什么）+ 参数回流
- 新建 `platforms/douyin.md`：**首次引入数据口径分级 🟢硬规则/🟡经验值/⚪待核验**
- `SKILL.md`：§0 三问 → **四问**（加平台档位）；新增「🔥 爆款改编」节（含平台模板）；
  新增红线 **R33 选题体检 / R34 钩子与包装显式标注 / R35 合规一票否决 / R36 AI标注三处+≥30秒**；
  第 1 步加 0/8b/8c 三项；参考表与常见坑速查同步

**关键落地细节（防违反既有铁律）**：
- 包装三件套**并入 `剧本分镜.md` 第一部分**，不新增 md（守「只留 4 个 md」）
- `check_viral.py` **只定契约不写实现**，等真实项目落地再写（避免死代码）

**留的未决事项**：① R11a「全程无配乐」与平台带 BGM 习惯的冲突（未擅自改）；② 单集时长档；
③ AI 标注 3 秒/5 秒两版口径（取严用 3 秒）；④ 母题库扩充。

## 用户定案：BGM 由后期自行添加

「不用加 bgm，我后期自己配」

- **R11a 保持不动**（生成侧永不配乐），BGM 明确**不属本链路职责**，已写成定案而非待决
- 连带产出一条实操约束：**音效不得满铺**（断续点状、低频克制、中高频让给台词），
  否则后期叠 BGM 会糊，且**音效已烧进轨里、后期救不回来**
- 落地位置：`SKILL.md` R11a 行、`references/prompt-format.md` §4 声音设计（新增对照表）、
  `platforms/douyin.md` §5（ⓘ 待决策 → 🟢 定案）、`references/viral.md` §12（未决表拆成
  「已决议 / 仍待决」，防以后翻案）、常见坑速查加一行

## 🔴 用户改默认画幅：9:16 → **16:9**（用户原话「我默认会做16:9」）

这是全局性改动，牵连资产尺寸、四视图构图、节奏档、提示词声明、发布平台匹配。已全面对齐：

| 位置 | 改动 |
|---|---|
| `SKILL.md` §0 第 1 问 | 默认画幅 → **16:9 横屏**，要 9:16 须显式说明 |
| `SKILL.md` 目标 / frontmatter description | 去掉「竖屏短剧」的默认暗示 |
| `SKILL.md` 第 2 步骨架、第 3 步资产表、gpt-image-2 尺寸 | 16:9 / `1536x1024` 置前为默认 |
| **`SKILL.md` R21** | 改为「默认 16:9；**改 9:16** 才需同步改尺寸/构图/声明/aspect_ratio/**节奏档**」 |
| **`SKILL.md` R6** | 拆成两档：**16:9 默认档**（无台词大分镜 ≤10 秒；单集 ≤90 秒，可声明放宽到 120 秒）/ **9:16 收紧档**（≤8 秒；≤90 秒） |
| `references/rhythm.md` | 硬性指标表改**双档对照表**（16:9 为默认列）；「竖屏 9:16（默认口径）」与「横屏 16:9（改画幅时适用）」两节**对调**，16:9 成默认节；§四 无台词口径改「4-10（16:9）/4-8（9:16）」；自检清单同步 |
| `references/character-consistency.md`、`naming.md` | 尺寸与四视图构图（横屏横向并排）改为默认口径 |
| `references/prompt-format.md` §画幅口径 | 表行对调，16:9 标默认 |
| `styles/guoman-3dcg.md`、`references/viral.md` | 适用画幅、时长档说明同步 |
| **`platforms/douyin.md`** | 🔴 加**画幅冲突警示**：本项目默认 16:9 与该平台 9:16 冲突；选它必须显式改画幅并换收紧节奏档，不要拿 16:9 素材直投 |

**我按「不一刀切」处理的一处**：画幅明确翻转，但**单集时长的数字没动**（仍是 ≤90 秒），
只把 120 秒记为「16:9 项目可在项目规范里显式放宽」的选项。理由：用户只说了画幅，
「默认16:9 → 所以单集可以到120秒」属于推断，容易踩上次「擅自扩大适用范围」的坑。
已在回复里点出来让用户定。

**画幅的连带传播要一并改（易漏）**：不止尺寸与构图，**依赖画幅档的所有口径都要跟着走**：
R6a（无台词豁免按档卡）、R7b（入场窗口与单镜时长）、R34（开场钩窗口）、
第 1 步算时长、常见坑速查、`viral.md` 的开场钩窗口/节拍表/自检/脚本契约。
**画幅就是一个全局档位开关，改它等于改半个技能。**

## 🔴 用户定：角色四视图**统一版式，与画幅解耦**

用户原话：「角色四视图应该统一一下，不管视频什么尺寸，角色四视图都是 16:9，
左边人物半身正脸照，右边分别是正视图全身，左视图全身，右视图全身」

**新规格（写进 `character-consistency.md` §角色四视图规格，为唯一权威）**：

| 项 | 值 |
|---|---|
| 尺寸 | **固定 16:9 → `1680x944`**（沿用文档里既有的 16:9 请求口径，未新造数） |
| 版式 | **横向四分格** ＝ `半身正脸`（约 1/3 宽）｜`全身正视`｜`全身左视`｜`全身右视` |
| 与画幅的关系 | **解耦**。改画幅**不用重出四视图**；只有关键帧/场景图跟着画幅走 |

**两处实质变化（旧版式 → 新版式）**：
1. 左格：**超大面部特写 → 半身正脸照**。附带修掉老坑「左格漏改服装」——
   半身含上装可直接验证；但**下装与靴只在右侧三视图可见**，校验要看两处（别误以为已全解决）。
2. 右侧：**正/侧/背 → 正/左/右**。背视图取消，改为左视+右视。

**顺带发现并纠正一处旧错误**：文档里把 `1536x1024` 当"横屏 16:9"是错的，
它是 **3:2**。16:9 一律用 `1680x944`。（`1680x944` 这个数本来就在文档里，
是"16:9 请求口径"，所以不是新造，是把它扶正为标准值。）

**连带改了**：`SKILL.md` 第 3 步表 / R21（四视图移出画幅联动清单）/ §0 第 1 问 /
gpt-image-2 尺寸行 / 图生图硬缺陷 / 常见坑速查（含新增 `1536x1024` 误用一行）、
`naming.md`、`prompt-format.md` §画幅口径（表加一列）、`styles/guoman-3dcg.md`
（新增注：版式是全局规格，**不要写进风格锁定串**）、`platforms/douyin.md` 冲突警示。

**待实测**：`1680x944` 是否被 gpt-image-2 网关接受（文档里记为请求口径，但我没实跑过）。






