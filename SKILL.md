---
name: rh-short-drama
description: 从小说或网文原文产出短剧/漫剧成片的完整生产链路（默认 16:9 横屏，可改 9:16 竖屏）。覆盖选题体检与爆款结构设计（钩子坐标、标题封面、平台档位、合规闸门）、小说改编分镜（大分镜小分镜两层结构、节奏控制）、视频提示词写作（秒级时间段落、三大段、d标签台词格式、参考图引用）、分镜关键帧生成（gpt-image-2）、视频生成（RunningHub rh-send 本地服务）。应在用户想改编小说或剧本为短剧、做爆款改编与选题拆解、写视频生成提示词（I2VA/T2VA）、生成分镜关键帧、或向本地 rh-send 服务提交视频生成任务时使用，包括做分集规划、写提示词、画分镜图、生成视频、重生成某单元等部分步骤。
agent_created: true
---

# RH 短剧生成

## 目标

从小说/剧本原文产出短剧成片（**默认 16:9 横屏**，可改 9:16 竖屏）。生产链路五环，**顺序不可颠倒**：

> 下图是**制作层**（做得出来）。**传播层**（选题体检、情绪钩子、标题封面、平台档位、合规闸门）
> 叠在它之上，见「🔥 爆款改编」节与 `references/viral.md`。**两层不冲突，叠加不覆盖。**

```text
剧本原文.md → 剧本分镜.md → 图片提示词.md / 视频提示词.md → 图片/（关键帧） → 视频/（成片）
```

| 环节    | 产出                                                     | 依赖    |
| ----- | ------------------------------------------------------ | ----- |
| 1 改编  | 剧本分镜.md **第一部分**（本集定位 / 改编说明 / 人物表 / 对白稿）              | 原文    |
| 1 分镜  | 剧本分镜.md **第二部分**（大分镜 + 小分镜 + 秒级时间码）                    | 第一部分  |
| 2 提示词 | 图片提示词.md（人设 + 场景 + 分镜三合一）· 视频提示词.md（每大分镜一个单元，**中英双块**） | 1     |
| 3 关键帧 | 图片/U##\_S##\_关键帧.png                                   | 2     |
| 4 视频  | 视频/U##\_S##-S##.mp4                                    | 2 + 3 |

**每一集一个文件夹**（EP01/EP02/…），共享层放全剧通用资产：

```text
<项目根>/
├── _共享资源/                       ← 🔴 只有这三个目录，不要新增编号
│   ├── 01_角色设定/        角色设定总表.md + gen_char*.py + CH##_四视图_<风格标记>.png
│   ├── 02_场景设定/        gen_scene.py + SC##_<场景名>.png
│   └── 03_风格与提示词规范/  风格选定.md / 平台选定.md / 提示词格式 / 节奏 / 命名 / 台词口径
├── EP01/
│   ├── 剧本原文.md  剧本分镜.md  图片提示词.md  视频提示词.md
│   ├── gen_shots.py  gen_video.py  dump_prompts.py  check_dialogue.py
│   ├── 图片/  视频/
├── _归档/                  旧文档 / 误归档废片（移动隔离，不删除）
└── 项目索引.md  引用技能.md
```

🔴 **剧集目录只留 4 个 md，多一个都算噪声。** 中间产物一律移入 `_归档/`。详见 `references/naming.md`。

🔴 **共享层只有 `01_角色设定` / `02_场景设定` / `03_风格与提示词规范` 三个目录**
（用户 2026-09-21 定）。**不要再新增 `00_` / `04_` 之类**——历史上文档里曾出现五目录结构，
导致「场景图到底放 02 还是 04」的长期歧义。**场景图放 `02_场景设定/`**。

分层判据：「是否全剧通用」，命名/风格/角色一律进共享层，不在分集目录重复。

> **R2a 的适用范围**：该禁令**只针对提示词文本**（真正投给模型的那段内容）。
> 本文档与 `references/` 里的说明性行文**不受此限**，照常使用中文标点即可。
> 唯一要注意的是：**不要照抄本文档代码块里的示例内容进提示词**（那里必然含 ❌ 反例）。

***

## 0. 开工前必问的六件事

**这六个口径不确认就动手，返工代价极大**（画幅改一次 = 全部图像资产重出；锚点模式换一次 = 关键帧资产与全部提示词的写法都要重做）。

| # | 问什么                                | 为什么要先问                                     |
| - | ---------------------------------- | ------------------------------------------ |
| 1 | **画幅**：**默认 16:9 横屏**（用户 2026-09-20 定）；要 9:16 竖屏须显式说明 | 决定关键帧/场景图的尺寸与整张节奏档位。**角色四视图不受影响**（固定 16:9）。改画幅见 R21 |
| 2 | **范围**：先做 EP01 还是先出全剧分集规划          | 决定要不要先切集                                   |
| 3 | **画风**：列出 **`role: main`** 的 `styles/*.md` 让用户挑（见下方「风格预设」） | 决定全套资产与提示词的风格锁定串。**这一问要排除 `overlay` 类**           |
| 4 | **Q 版整活**：问「**是否启用 Q 版整活镜头**」（`role: overlay` 的 `styles/*.md`） | 决定要不要出 Q 版角色资产、分镜要不要留整活位（R37）。**必须先问完主画风再问这个** |
| 5 | **平台**：列出 `platforms/*.md` 让用户挑（见下方「爆款改编」） | 决定时长档、节律档、标题规则与审核尺度（R36）                    |
| 6 | **锚点模式**：问「**视频是否固定分镜锚点**」——**固定**＝出关键帧、按 I2VA 投喂（原方案）；**不固定**＝不出关键帧，只投角色四视图 + 场景图，靠提示词逐秒锁死画面（见下方「锚点模式」） | 决定要不要出关键帧资产、提示词写到多细、投喂几张图。**这是全片级口径，中途换代价极大** |

> 🔴 **第 3、4 问有严格先后**：先问主画风（排除 Q 版），**再单独问要不要 Q 版整活**。
> 顺序反了，用户会把 Q 版当成一个"可选的主画风"来选，选完发现整片都是 Q 版。

### 单集怎么切：用台词量反推，不要凭感觉

**一集的容量由台词量决定，不由剧情自然段落决定。**

```text
① 脚本抽出该区间全部直接引语，按含标点字数 ÷ 4.5 算台词总时长
② 允许的单集总时长 ≤ 台词总时长 ÷ 0.55（R7a 反推）
③ 若算出来超过 90 秒上限 → 必须砍掉部分剧情，把切点前移
```

**踩坑实录**：某项目原文 197 行处有个自然的剧情断点，凭感觉切在那里，
一算台词 78.9 秒，90 秒单集只剩 11 秒画面，第一集连开场都铺不开。
前移到 149 行（台词 52.7 秒 / 单集 78 秒 / 占比 67.7%）才站得住。

> **判据**：切点附近若是"台词密集区"，它就是个坏切点；台词稀疏处才好收尾。
> **不许为了留住剧情而压缩台词，也不许为了凑集数而加台词。**

***

## 🎬 锚点模式（开工第 6 问）

**一句话**：分镜关键帧到底**出不出、投不投**。
**这是全片级开关**，开工时问一次，选定后写进项目规范、全剧沿用。

| | **固定锚点**（默认 · 原方案） | **不固定锚点**（2026-09-21 新增） |
|---|---|---|
| 关键帧 | ✅ 出 `U##_S##_关键帧.png` | ❌ 不出 |
| 投喂素材 | 关键帧 1 + 角色四视图 1-3 + 场景图 1-2 | 角色四视图 1 + 场景图 1（+ 道具图） |
| 提示词写法 | 常规（`prompt-format.md`） | 🔴 **必须写到「图里看不出的信息全有」** |
| 画面稳定性 | 高（关键帧锁画面基调） | 中（只靠文字常量锁，人物脸与空间会漂） |
| 省掉的环节 | — | 第 4 步「生成分镜关键帧」整步 |
| 适用 | 人物一致性要求高、关键帧来得及出 | 想省掉出图环节、试跑新画风、关键帧来不及出 |

### 不固定锚点的九条纪律

1. **锁定段加厚**：场景锁定写空间 / 陈设 / 光照方向 / 色调；人物锁定写面部 / 发型 / 体型 / 服装 + 负面约束。每个单元**逐字复用**同一段，不换同义词（R8c）。
2. **素材恒定、编号一致**：全部单元统一 `（@图片1）` = 主角四视图、`（@图片2）` = 场景图（有道具再加 `（@图片3）`）。脚本好复用，人也容易逐单元对照。
3. **秒级段落写到「可执行」**：每段必须有 **景别 + 机位轨迹 + 幅度 + 速度 + 材质 + 动作受力 + 表演层次（情绪起点→终点）**。只写动作骨架一定平淡。
4. **画面文字仍走后期**：面板文案、字幕、AI 标注一律不进提示词。要显示图案就投**道具图**，并把它的身份在提示词里锁死为「屏幕上的一张低分辨率贴纸，不得影响人物、房间与整体渲染风格」，防止画风污染（R27a）。
5. 🔴 **系统说的话（原文【】内容）要「双轨落地」：口播 + 写进画面**（用户 2026-09-21 口径：「系统说的话是要写进画面的」）。
   - **成句的系统台词**：进 `<d>` 口播（音色＝系统音），**同时**在面板 / 屏幕上以**发光文字显示**。显示要写进提示词：**居中横排、现代无衬线、字号大到一眼可读、逐句照原文不简化**。
   - **数值型提示**（`【愤怒值+10】`）：只需**画面弹字 + 音效**，不必口播。
   - ⚠️ **模型生成中文长句有糊字风险**（几十字的长句尤其危险）。所以提示词里**位置与排版必须写死**；一旦成片糊字，**兜底＝把文字内容换成「一行发光的占位字符条」，实际文案由后期叠到同一位置**（位置不变才能对准）。
   - 项目若要求连数值也念，须在项目规范里显式声明并重算单集容量。
6. **验收与回退**：漂移风险高于固定锚点。成片若人物脸或空间明显漂，**回退＝补出该单元关键帧、改走固定锚点重跑**；不要靠反复改文字硬调（R22 / R30）。
7. 🔴 **必须写「空间调度四件套」（防穿帮）**：用户 2026-09-21 实测要求「视频提示词要描述人物具体位置，避免镜头穿帮」。**无锚点时这是防穿帮的主力，有锚点时可以薄**。四件套缺一不可：
   | # | 写在哪 | 写什么 |
   |---|---|---|
   | ① | **Scene lock** | 房间地理：门 / 窗 / 床 / 桌的相对方位，以及「**他的床位是哪一个**」（如「左墙那组的靠窗下铺，唯一铺了寝具的那张」） |
   | ② | **Character lock** 里的 **Blocking** | 他坐在**哪张床垫的外沿**、面朝哪、双脚在哪、手肘在哪、**上身不离开那张床** |
   | ③ | **Editing and action** 末尾的 **Space rules** | 机位边界：**不越墙、不穿床架与家具、不绕到背后、不进入道具占位**；道具（面板等）与他的相对位置 |
   | ④ | **Assets** 段的 **Continuity** | 「画面里只有他一人」、其余床位/陈设状态全程不变、**身体与投影不得穿过床架/家具/墙壁**、无多余家具与镜像肢体 |
   > **为什么有效**：模型在无锚点时最容易犯的两类错是「人物瞬时位移」（上一镜坐床沿、下一镜站门边）与「穿模」（手臂穿进床架）。四件套分别锁死**他的空间锚点**与**机位的活动边界**，把这两类错压到最低。
8. 🔴 **提示词里「一段 = 一镜」，禁止「大分镜套小分镜」**（用户 2026-09-21 实测要求：「视频提示词不要大分镜包含小分镜，每个分镜都是不同的时段」）。
   - **每个秒级段落 = 一个镜**：段头写这一镜的 `时间段 | 焦距 | 运镜方式`，段内**不再出现切点**——不许在段落中间写「第 2.2 秒切镜到…」这种层叠结构，那是「大分镜套小分镜」，模型会读成一次生成里切多刀。
   - **硬校验**：`该单元的时段数 == 该单元在逐镜表里的镜数`，且**时段边界必须与镜边界逐一对齐**（相对时间）。改完必跑脚本比对，不要靠人眼。
   - **跨镜长台词怎么办**：一条完整台词按 R3 不得跨段拆。若它跨越了逐镜表的多个镜，就**把这几个镜合并成一个镜**（`[豁免:长镜]`，理由写「台词容量」），让 `段数 == 镜数` 重新成立。**绝不为了保住镜数去拆台词**——拆台词会吞字或接缝停顿。
   - **合并后要一起报账**：`[豁免:长镜]` 是 R6a 的机制，需同时报「豁免时长 / 单集占比」是否仍在上限内（≤2 个、累计 ≤单集 20%）。若略超，说明**驱动是台词容量（R4 不得硬压语速）而非画面铺陈**，并指出豁免时长里有多少秒本身就在说台词——这比数字本身更能说明问题。
   - **「一段内的表演节点」不算切镜**：长镜内部可以有「约第 4 秒他抬手」这类节拍描述，但必须用**「一镜到底、段内不切」**在段头写死，否则模型会自己找地方切。
9. 🔴 **禁止远景，一切镜头围绕人物**（用户 2026-09-21 口径：「尽量避免超远分镜，分镜最好围绕人物展开，**如果人物距离镜头过远会渲染崩掉**」）。
   - **人物占画幅高度 ≥1/3**，机位与主体的距离 **≤3 米**。写分镜和写提示词时都要按这条自查。
   - **禁用 18mm / 24mm 超广角，禁用「全景 / 大远景」**。最远到**中景**（50mm）。
   - **需要交代空间尺度时，用「过肩 + 纵深失焦」代替拉远**：把人物或其**肩 / 后脑 / 前景肢体**留在画面当锚点，环境压进**失焦的纵深**、只给光斑与轮廓色块，不给清晰细节。这样既有空间信息，人又离镜头近。
   - 🔴 **「拉远」是陷阱**：想表达「空、冷、渺小」时，第一反应常是拉远镜头，但那正是渲染崩掉的写法。改用**光影消失 + 背景失焦**来表达同样的意思。
   - **没有人物可锚定的纯环境过场镜（空房间、空大厅、地形远景）一律删掉**，或改挂到人物身上。
   - **不因此丢掉信息**：环境细节不是不能出现，而是**留在失焦纵深里**；观众要的是「他在什么处境里」，不是一个清晰的空场。
   - **正例**：`中近景·50mm·微横移` — 他在画面左三分之一占画幅高度近一半，另外三张空床只作为他身后的失焦纵深。**反例**：`全景·24mm·横移` — 镜头沿空地横移，人物在纵深里很小。

> 🔴 **模式写进 `_共享资源/03_风格与提示词规范/项目规范.md`**，全剧沿用。
> 中途换模式要显式声明，并说明**已生成单元是否重跑**。

***

***

## 🖥 屏幕文字 / 系统面板：先出「面板图」，再作为参考图投喂

**用户口径（2026-09-21）**：遇到需要展示**系统面板 / 屏幕文字**的分镜，
**先生成写着文字的面板图（无额外背景）**，再把这张图作为该分镜的**参考输入**——
**不再让视觉模型主导文字的排版**（视觉模型的文字排版没有图像模型稳定）。
**这条与锚点模式无关，固定 / 不固定锚点都适用。**

| 步 | 做什么 |
|---|---|
| 1 | 盘点本集有哪几个**面板状态**：**一个文案 = 一张图**，另加需要的**空白态** |
| 2 | 写 `EP##/gen_panel.py`（照 `_共享资源/02_场景设定/gen_scene.py` 的凭证 / 代理 / 模型链路搬一套，支持 `--list` / `--force` / `--dry-run`） |
| 3 | 面板图落 **`EP##/图片/PANEL_<台词编号>_<内容短名>.png`**——**按集存放，不进 `_共享资源/`**（共享层只有 01/02/03 三个目录） |
| 4 | 进对应单元的**素材映射**（`（@图片N）`）；核对**单单元参考图 ≤5 张**（服务端硬上限，超了返 400） |
| 5 | 秒级段落里**删掉文字内容与排版描述**，改成「照（@图片N）原样贴上、**不重排不改字**」 |

### 出图提示词的四条硬要求

1. **纯黑底、面板居中**：明确写「除面板外画面其余部分必须是**纯黑**——没有房间、桌面、地面、手、人物、背景物体、环境光、倒影」。
   图像模型**出不了真正的透明通道**，纯黑底 + 投喂时按屏幕叠加是最稳的做法。
2. **文字逐字准确**：把原文**单独成行**给出，并写「不得增字 / 删字 / 改字 / 错字；不得出现乱码；不得出现英文、拼音、数字编号；不得出现任何多余文字、副标题、按钮、图标、水印」。
3. **面板本体规格写死**：正对镜头、**无透视无倾斜无旋转**、边框 / 玻璃质感 / 发光色一次写清；**多张面板共用同一段「面板本体」描述，只换文字**，否则几张面板会长得不一样、拼不到一起。**面板尺寸也要写死**（如「宽约 88%、高约 76%」），否则行数一多面板会被压扁、字跟着变小。
4. 🔴 **文字排版：分行写、字大、字粗、不变形**（用户 2026-09-22 实测反馈：「**文字不要光挤在一排写那么长，字又小又细又长，不容易观看**」）。
   - **必须分行**：把文案拆成 **2-5 行**（每行 **10-15 字**，按标点 / 短语断句），提示词里**逐行列出**（「第一行：… / 第二行：…」），并写「**不要合并成一行、不要自己重新断句**」。
     🔴 **把几十字的文案一次性塞成一行**，模型就会把字压小、压细、拉长——这正是要避免的排法。
   - **字要大**：写「**在不超出面板边界的前提下把字放到最大**，整块文字的高度约占面板高度的 **60%-80%**、宽度不超过 **92%**」。只写「字号大到一眼可读」约束力不够。
   - **字要粗**：指定**中粗（Medium / SemiBold）无衬线黑体**，并显式否定「**不要细体、不要 Light / Thin 字重、不要细长发虚的笔画**」。
   - **字形不得变形**：写「不得横向拉长、纵向压扁、倾斜或做任何拉伸变形；不要艺术字、不要手写体、不要描边空心字」。
   - **行距舒展**：约半个字高的间隙；各行水平居中、左端对齐成一块整齐的长方形文字块。

### 🔴 投喂侧必须声明「黑底视为透明」

面板图是纯黑底，视频模型**会**把黑底当实心色块画进画面（就是一个黑框）。所以在**段落与 Assets 两处**都要写明：
**「把（@图片N）里的纯黑底视为透明，按屏幕叠加的方式贴上去；不要把黑底画进画面，也不要在黑底范围里补房间、桌面或环境。」**

### 风险与兜底（与「后期叠字」那条路的区别）

- 风险点**从视频模型前移到图像模型**：错字、漏字、乱码、多出副标题。
- **兜底＝重出那张面板图**（`python EP##/gen_panel.py PANEL_XX --force`），**不是**后期叠字。
- 仍要警惕第二类风险：**视频模型贴图时把面板拉伸 / 裁切 / 重绘**，所以「不重排、不改字、按屏幕叠加」要写死在段落里。

> **正例**：`0—2.5s | 85mm | 固定、微推：面板的边框、玻璃与正中那个内凹的方形卡槽，全部照（@图片4）原样贴上（黑底视为透明、按屏幕叠加），不重新绘制、不加任何文字。`
> **反例**：`…面板玻璃上浮出一行发光的中文、居中、现代无衬线字体、字号大到一眼可读：支付成功，恭喜宿主成功绑定人生背景编辑器。`（把文案与排版交给视频模型 = 糊字）

***

## 🎨 风格预设（`styles/`）

**画风是开工第一件事，且必须由用户选。** 预设风格都放在技能目录的 `styles/` 下，
**一个 md 一种风格，文件名（去掉 `.md`）就是 `style_key`**。

### 🔴 风格分两种角色：`main` 与 `overlay`

风格文件 frontmatter 里的 **`role`** 字段决定它怎么被用：

| role | 含义 | 怎么选 | 现有 |
| -------------- | ------------------- | --------------- | --- |
| **`main`** | **主画风**：定全片基调，**单选、互斥** | 开工第 3 问，列出来让用户挑一项 | `guoman-3dcg` |
| **`overlay`** | **叠加画风**：插播用，**可与任意主画风共存** | 开工第 4 问，**单独问是否启用** | `chibi-gag`（Q版整活） |

**两个 role 的用法差异**：

| | `main` | `overlay` |
|---|---|---|
| 覆盖范围 | 全片基调 | **只覆盖它自己的那些镜头** |
| 资产 | 全套（四视图/场景图/关键帧） | **只有角色 Q 版形象**（场景不需要） |
| 能否多选 | **不能** | 可以（但每多一套资产就多一份工作量） |
| 与另一 role 的关系 | — | **不替换主画风，只插播** |

### 开工时怎么问

```text
① 扫 styles/*.md（跳过 `_` 开头的文件），按 role 分两组
② 第 3 问：只列 role=main 的，让用户挑一项。**不要替用户默认**
③ 第 4 问：只列 role=overlay 的，问「是否启用」。
   ⚠️ 顺序不能反，也不能合并成一次多选（会让用户以为 Q 版是个可选主画风）
④ 选定后：style_key 记进项目规范，后续资产与提示词按对应文件的锁定串走
⑤ 用户想要的风格不在清单里 → 按下面模板新建，**先跟用户对齐特征再落盘**（别忘了填 role）
```

**已有预设**：

| style_key | 显示名 | role | 文件 |
| --------- | --------------------------------- | ------ | ----------------------- |
| `guoman-3dcg` | 国漫 3D CG（次表面散射 CG 电影质感） | `main` | `styles/guoman-3dcg.md` |
| `chibi-gag` | Q版整活（头身比 1:2 的插播镜） | `overlay` | `styles/chibi-gag.md` |
| ⬜ 待定 | 古风 CG（厚涂感，历史项目用过） | `main` | ⬜ **待建**（资产标记 `古风CG`；特征待补） |

> 🔴 **风格 ≠ 版本。** 这是最容易糊掉的一处：
>
> | 是什么 | 长什么样 | 怎么处置 |
> | ------- | -------------------------- | -------------------------------- |
> | **两种风格** | `_3DCG版` 与 `_古风CG版` | **各自一个预设文件、各自的锁定串、各自的参考图目录**，互不替代 |
> | **同一风格的两个版本** | `U01_S01-S04.mp4` / `_v2.mp4` | 只有 `_v2` / `_v3`，是重生成，**与画风无关** |
>
> `_3DCG版` / `_古风CG版` 这些**标记不是版本号，是风格标识**。
> 所以不要用"命名兼容 / 旧版沿用"这类口径把两套画风糊成一套。

### 一个风格文件必须给全的六样东西

1. **`role`**（`main` 主画风 / `overlay` 叠加画风）
2. **风格特征**（分维度写清"要什么 / 不要什么"）
3. **风格锁定串 · 中文**（给图像提示词：四视图 / 场景图 / 关键帧）
4. **风格锁定串 · 英文**（给视频提示词，R11 正文英文）
5. **负面约束（NEG）**
6. **落地口径**：资产命名标记、参考图子目录名、角色表字段名

> **只给一段散文描述是不够用的。** 缺了中英锁定串，写提示词时就得临场发挥，
> 而临场发挥 = 风格漂移。**风格串要能整段照抄**。

### 新建风格文件的模板

````md
---
style_key: <小写英文连字符>       # 脚本 --style 取值，也是文件名
name: <中文显示名>
role: main | overlay              # main 定全片基调（互斥）；overlay 插播（可共存）
asset_tag: <资产命名标记>          # 四视图后缀，如 3DCG → CH01_四视图_3DCG版.png
ref_subdir: <关键帧子目录>          # 如 ref3d → 图片/ref3d/
char_field: <角色表字段>            # 如 file_ref3d
created: <YYYY-MM-DD>
---

# 风格：<显示名>

## 1. 一句话定位
## 2. 风格特征（分维度：渲染路线 / 皮肤 / 五官 / 毛发 / 服装材质 / 光照 / 色调 / 镜头质感）
## 3. 风格锁定串
### 3.1 图像用 · 中文尾串
### 3.2 视频用 · 英文风格层
## 4. 负面约束（NEG）
## 5. 互斥项（最容易混进来的错画风）
## 6. 落地清单
## 7. 验收清单（只做机械核对）
````

> 🔴 **新建风格时不要删旧风格文件**。风格并存留档，可对照，"哪套更好"由用户裁决。

### 换风格的连带影响（别漏）

| 项 | 后果 |
| - | ------------------------------------------------------------ |
| 图像资产 | 四视图 / 场景图 / 关键帧**全部重出**，不要新旧混用（R21） |
| 投喂参考图 | **风格层与投喂的参考图必须同风格**，只改文案不改图 = 模型被旧图带偏（R27a） |
| 视频提示词 | 英文正文**自带**风格口径 → 切风格要改英文风格层，**中文正则替换对英文无效**（R32） |
| 字数闸门 | 风格串长短会顶字数预算（R10 / R26），换串后重跑字数自检 |
| **Q 版资产** | **换主画风时，Q 版也要跟着重出**——Q 版靠"像主画风角色"才成立，主画风一换它就不像了（R37） |

***

## 🔥 爆款改编（传播层）

**制作层红线 R1-R32 管"做得出来"，本节管"有人看"。**
详细规范在 **`references/viral.md`**，本节只给机制与挂载点。

| # | 能力模块 | 解决 | 产物 |
| - | ---------- | ------- | ------------------ |
| 1 | **选题拆解** | 值不值得做 | 六项爆款基因体检（≥8/12 才开工） |
| 2 | **结构模板** | 骨架稳不稳 | 五个母题骨架 + 秒级节拍表 |
| 3 | **情绪钩子工程** | 会不会被划走 | 开场钩 / 结尾钩 / 情绪节点坐标 |
| 4 | **标题与开头包装** | 点不点进来 | 标题、封面首帧、简介 |
| 5 | **平台调性适配** | 投给谁看 | `platforms/<platform_key>.md` |
| 6 | **合规与过审** | 会不会被驳回 | 一票否决清单 + AI 标注位 |
| 7 | **数据复盘迭代** | 下一集怎么改 | 指标归因表 + 参数回流 |

> **前六项决定"这一集能不能爆"，第七项决定"能不能稳定爆"。**
> 缺第七项，能力就停在运气上。

### 平台档位（`platforms/`）

机制与 `styles/` **完全对称**：一个 md 一个平台，文件名（去 `.md`）= `platform_key`。

```text
① 扫 platforms/*.md（跳过 `_` 开头的文件），列出「文件名 + 显示名」
② 让用户挑一项；**没有就按模板新建**，先跟用户对齐再落盘
③ 选定后 platform_key 记进 `_共享资源/03_风格与提示词规范/平台选定.md`
```

**已有档位**：

| platform_key | 显示名 | 文件 |
| ------------ | ---- | ---------------------- |
| `douyin` | 抖音 | `platforms/douyin.md` |

**一个平台文件必须给全的六样**：① 画幅与时长档 ② 节律档 ③ 标题/封面规则
④ 审核尺度 ⑤ 音频口径 ⑥ **数据口径分级**。

### 🔴 数据口径必须分级（否则会把经验值当法规）

| 级别 | 含义 | 处理 |
| ------- | ------------- | ------------------- |
| 🟢 硬规则 | 法规/平台明文 | 必须遵守，违反 = 驳回/下架 |
| 🟡 经验值 | 行业实践统计 | 可作默认，**必须项目内自测校正** |
| ⚪ 待核验 | 来源不明或可能过时 | **不得作为决策依据**，先查证 |

**平台规则会变，且各平台表述不一致**（例：AI 标注位有"片头前 3 秒"与"前 5 秒"两种口径）。
以平台创作者后台当前官方口径为准，`platforms/` 里记的是**快照 + 日期**。

### 新建平台档位的模板

````md
---
platform_key: <小写英文>
name: <平台显示名>
created: <YYYY-MM-DD>
snapshot: <快照日期>          # 规则会变，标清取证时点
---

# 平台档位：<显示名>

## 1. 画幅与时长档
## 2. 节律档（钩子间隔、卡点位置）
## 3. 标题与封面规则（含禁用词）
## 4. 审核尺度
## 5. 音频口径
## 6. 数据口径（逐条标 🟢/🟡/⚪）
## 7. 本平台的自检补充
````

### 挂载到现有链路

| 现有步骤 | 新增动作 |
| ------------- | ---------------------------------------- |
| 第 0 步 开工四问 | 加问**平台档位** |
| 第 1 步 改编分镜 | 先跑**选题体检**；切分镜时按节拍表落**钩子坐标与标注** |
| 第 1 步 自检 | 开场钩/结尾钩/情绪节点标注齐全；首次反转 ≤30 秒 |
| 第 2 步 提示词 | **U01 预留 AI 标注位**；开场 3 秒三层钩落进秒级段落 |
| 第 3/4 步 资产 | 封面帧 = 关键帧 S01，按封面规则校验 |
| 第 5 步 提交 | 跑合规闸门与包装三件套检查 |
| 第 6 步 拼接 | 顺带核验**单集 ≥30 秒**（合规硬下限） |
| 发布后 | 指标归因表 + 参数回流到 `platforms/` / `styles/` |

> 🔴 **不新增剧集目录的 md**：包装信息（标题/封面/简介/AI 标注文案）
> **并入 `剧本分镜.md` 第一部分**，守住「只留 4 个 md」的铁律（R34）。

***

## 工作流程

### 第 1 步：改编成剧本分镜

逐条执行，产出 `剧本分镜.md`：

0. **选题体检**（R33）：候选区间跑六项爆款基因打分，≥8/12 且「可视化冲突」「台词密度」
   均非 0 分才往下走。**先定母题骨架**（打脸逆袭／身份揭秘／重生先知／亲情认亲／悬念威胁），
   它决定首次反转该落在第几秒。
1. **台词提取**：从原文摘出全部直接引语进台词表，**一字不改**。
   字数用脚本数（`len(台词)`，含标点），不凭感觉标。
2. **心理描写转表演**：原文内心独白 → 转成「张嘴→喉咙动→话咽回→目光移开」
   这类**表演动作链**。绝不写成叙述性画外音。
3. **叙述转画面**：地理/环境描写 → 画面呈现或删除，不加解释性旁白。
4. **砍无台词剧情**：现有台词不改；占比不足 55% 时，**删/压无台词的剧情段**
   （删镜 → 压时长 → 合并信息），**不要靠加台词堆占比**。
5. **切大分镜**：4-15 秒一个，**超 15 秒必须拆成独立大分镜**（见红线 R5）。
   拆分点优先句子边界（按台词分配），其次动作完成瞬间。
6. **切小分镜**：大分镜内 0.8-6 秒一个，全片连续编号 S01/S02…
6b. **插整活镜**（**仅当第 4 问启用了 Q 版整活**，见红线 R37）：逐镜过一遍，命中三类之一就插一镜，
   **独立成大分镜**（不与主画风镜头同单元），并标注 `[整活:科普]` / `[整活:风险]` / `[整活:心理]`：
   - **科普**：世界观／规则／设定背景，正剧讲会拖节奏的，用 Q 版图示带过
   - **风险**：**画面尺度类**风险镜（血腥／伤害／刀具／危险动作）→ Q 版小人 + **字面梗**替代，
     **必须写明替代了什么**（写法见 `styles/chibi-gag.md` §6.1）
   - **心理**：搞笑向心理活动／吐槽，替代内心独白
   🔴 **红线不可用整活规避**：私刑复仇、迷信成真、未成年成人化这类**价值观**红线，Q 版包装照样违规，
   **必须改剧情**（判断法见 `styles/chibi-gag.md` §6.2）。
7. **算时长**：有台词段 ≥ 台词时长 + 表演时间；无台词段压到**本画幅档的上限**
   （16:9 → 4-10 秒 / 9:16 → 4-8 秒）；**铺垫镜 / 长镜头可豁免**，须显式标注（见红线 R6a）。
   整活镜按 `rhythm.md` 的整活档计数与控量。
8. **标情绪**：每个大分镜标注情绪的**起点与终点**，以及心理外化的动作。
8b. **落钩子坐标**（R34）：标开场钩、结尾钩、每 20-30 秒一个情绪节点；
   核验**首次反转 ≤30 秒**、**无连续单向施压 >30 秒**。
8c. **写包装**（R34，并入第一部分）：标题 ≥3 候选 + 封面帧（默认 U01_S01）+ 简介
   + AI 内容标注文案。
9. **自检**：时间码首尾相接、大分镜无超限、原文台词未删、台词占比 ≥55%、
   节奏无纯过场、钩子标注齐全、单集 ≥30 秒（R36）、平台档位与合规红线已过（R35）、
   整活镜数量与时长在档内且整活标注齐全（R37）。

### 第 2 步：写提示词

一个大分镜 = 一个生成单元 = `视频提示词.md` 里的一个 `# U##` 章节。
**章节内并列两个块**：`### 中文` 在前、`### 英文` 在后，只差语言、逐句对应。

**先读** **`references/prompt-format.md`**，按其结构写作。
**风格层取自开工选定的风格文件** `styles/<style_key>.md` 的 §3.2 英文风格层，**整串照抄**
（不要只挑几句，风格串拆着用会出来半套画风）。风格层与投喂的参考图必须同风格（R27a）。
**🔴 正文一律英文**（R11，2026-09-18 起）：场景、秒级段落、镜头调度、三大段全用英文；
只有台词（`<d>[Chinese] …</d>`）、参考图引用（`（@图片N）`）与元数据表保持原样。

结构如下：

```text
生成一段<N>秒、16:9、2K、原生立体声的<风格>短片。<场景与资产锁定>

素材：（@图片1）为<角色>四视图；（@图片2）为<场景>参考图；…

0—0.8秒：<场景+人物动机+动作+情绪>。镜头<轨迹+幅度+速度>。
0.8—9.3秒：…<心理外化细节>，用清脆的童音一气说完：<d>[Chinese] 台词。</d>…

剪辑与动作：<切点逻辑+受力传递链+节奏>
视觉风格：<渲染质感+材质+光照+色调+景深>
声音设计：<声道分层+音效材质/距离/力度> 全程无配乐
```

核心是 **R8b 逻辑描述优先**：写清人物动机、情绪走向、空间关系、事件因果，
让模型决定这场戏怎么调度；**同时必须投喂分镜关键帧作参考**（R8a）。
参考它稳定人物、场景与画面基调，但不要求逐帧复刻。

**每个单元**还需要（供脚本解析，结构见 `references/naming.md`）：

- 投喂信息表（模式/时长/台词/图片参考）
- 素材映射表（首列 `（@图片N）`，第二列文件名带扩展名）
- 台词与时长核算表

### 第 3 步：生成场景参考图与角色四视图

两类资产都要有，缺一不可（见红线 R8）：

| 资产        | 位置                               | 规格                                                                      |
| --------- | -------------------------------- | ----------------------------------------------------------------------- |
| **角色四视图** | `_共享资源/01_角色设定/CH##_四视图_<风格标记>.png` | 🔴 **固定 16:9 → `1680x944`，与成片画幅无关**；**横向四分格** = `半身正脸` ｜ `全身正视` ｜ `全身侧视` ｜ `全身背视`，纯色背景、无边框无分隔线；**风格标记取自所选风格文件**（`guoman-3dcg` → `3DCG`） |
| **场景参考图** | `_共享资源/02_场景设定/SC##_<场景名>.png`    | **与成片画幅一致**（16:9 → `1680x944` / 9:16 → `1024x1536`）；锁建筑形制、地貌、材质、光照基调；同一场景全剧复用一张                                        |
| **Q 版四视图**（仅启用整活时） | `_共享资源/01_角色设定/CH##_四视图_Q版.png` | **版式同全局规格**（四分格 `1680x944`）；**从主画风基准件参照派生**，保留发色/瞳色/服饰配色/标志性配饰，只改比例为头身比 1:2。**只给会出场整活镜的角色出**（R37） |

**两类资产的提示词都要挂所选风格文件的「中文锁定串」**（如 `styles/guoman-3dcg.md` §3.1），
**逐字复用、不改写措辞**。

#### 🔴 人设取证：先跑脚本，别凭印象写外貌

网文/剧本**对话密集，绝大多数配角没有外貌描写**。凭印象写 = 后面全组返工。
用两级脚本取证（实测有效，某项目 21 个配角一次过）：

| 级 | 做什么 | 判据 |
|---|---|---|
| **一级：批量扫** | 按角色名 + **别名**取 ±55 字窗口，分「登场」（前几次出现）与「外貌/着装/年龄」（关键词过滤）两类，每角色上限 ~10 条 | 一眼能定「年龄/性别/职业/性格」的写进常量 |
| **二级：定点放宽** | 拿不准的角色单独跑，窗口放到 ±260 字 | 🔴 **专治身份 / 性别 / 关系歧义** |

> 🔴 **二级不可省，实测靠它抓出三处会毁掉整条线的误读**：
> ① 某「院长」被当男性处理，原文有「**老太太**」「**让她**忘乎所以」；
> ② 某配角被当成主角的叔叔，实际是**反派方的儿子**（拨给儿子的电话被误读）；
> ③ 某场景按「室内演播厅」出图，原文节目其实**全程外景直播**。
> **前三处若只看一级摘要，都会漏。** 拿不准的一律二级放宽再定。

> ⚠️ **别名必须列全**：漏一个别名就漏掉一批片段（如「老板」会被反复用来指代多个不同角色，
> 要写清是哪一个）。**同名不同人时按上下文人工裁决，不要自动合并。**

#### 批量出图：单任务串行 3-4 张 + 并行 6 个任务

**推荐档位**（实测比「每张一个任务」调度更省、也不触发限流）：

```bash
# 一个后台任务内串行 3-4 张，同时开 6 个这样的任务
python gen_char_3dcg.py CH04 CH05 CH06 CH07 > tools/gen_w1.log 2>&1
python gen_char_3dcg.py CH08 CH09 CH10 CH11 > tools/gen_w2.log 2>&1
...（共 6 个）
```

- 单任务实测 2m28s–5m06s（串行 3-4 张）；一批 24 张角色 + 12 张场景约 **12 分钟**
- **日志必须一任务一文件**，不要多个任务写同一个日志（并发会串）
- 脚本里的 `print` 要加 `flush=True`，否则后台跑时日志长时间空白、看不出进度
- 用 `--list` 在**提交前**核对「单元数 / 提示词字数 / 已存在状态」三项，通过了再提交

**角色若在剧情中有多套装束**（寝衣 / 朝服 / 帝王 / 现代…），**必须逐套出图**，
不能只有一张便服。多形象一律走**图生图**从基准件派生，禁用纯文生图
（详见 `references/character-consistency.md`）。

详见 `references/character-consistency.md`。

### 第 4 步：生成分镜关键帧（必需）

关键帧**是视频生成的必备参考**，不能跳过。
用来稳定人物状态、空间关系、光线基调与画面风格，**是参考基准，不是逐帧模板**。

**图像生成提示词必须过「逻辑 + 一致性」两关**（红线 R8c）：不能只堆视觉形容词，
要保证物理/空间/时间/道具/世界观自洽，且角色常量、场景词、画风尾串**逐字复用**。
详见 `references/character-consistency.md`。

### 第 5 步：生成视频

1. **探测服务**（不要自己启动，见红线 R13）：
   `GET http://127.0.0.1:8282/docs`，不通就提示用户启动并停止。
2. 脚本读取 `视频提示词.md`（**默认取英文块**）→ 上传参考图（**关键帧为必备参考**，角色四视图 + 场景参考图同步投喂，
   走 I2VA）→ 提交 → 每 20 秒轮询 → 等落盘。
3. 成功后把文件从 `save/` 复制到 `EP##/视频/` 并按规范重命名。

### 第 6 步：拼接整集（用户要"完整成片"时必做）

**本机 PATH 里通常没有 ffmpeg**，用 `imageio-ffmpeg` 自带二进制：

```bash
python -m pip install imageio-ffmpeg --proxy http://127.0.0.1:7897
```

```python
import imageio_ffmpeg
exe = imageio_ffmpeg.get_ffmpeg_exe()      # .../imageio_ffmpeg/binaries/ffmpeg-*.exe
```

拼接两条路，先快后稳：

| 顺序 | 命令                                                                                                       | 说明                      |
| -- | -------------------------------------------------------------------------------------------------------- | ----------------------- |
| 1  | `-f concat -safe 0 -i list.txt -c copy`                                                                  | 无损、秒级完成；**要求各片段编码参数一致** |
| 2  | `-filter_complex "[0:v][0:a][1:v][1:a]…concat=n=N:v=1:a=1[v][a]" -c:v libx264 -pix_fmt yuv420p -c:a aac` | 回退方案，重编码统一参数            |

**纪律**：拼接前按单元编号校验片段齐全，缺任一单元就**不要出片**
（半集成片比没有成片更容易被误当成交付物）；输出先写 `.part.mp4` 再 `os.replace`，
避免中途失败留下一个看似完整的坏文件。

🔴 **成片一定会比分镜表上的目标时长长，要如实报出来**：
每段实际生成时长都比提交的 `duration` 长约 0.1—0.6 秒，**20 段累计能把 175 秒放大到 184 秒（+5%）**。
这是上游的正常浮动，不是故障。所以：

- 拼接前**逐段核验**（`ffmpeg -i` 读 Duration，与目标比对，容差 ±2 秒），
  顺便能提前抓出「只生成了一半」的坏片段；
- 交付时**报实测成片时长**，不要让用户以为成片就是分镜表上那个数。

***

## 多风格并存（A/B 两套口径怎么放）

一个项目可能先后确立多套画风口径（例：先按通用提示词文本归纳的古风CG，后按成片截图反推的 3DCG）。
**不要覆盖重来，并存留档、可对照**，因为"哪套更好"最终由用户裁决。

🔴 **每套画风在 `styles/` 里各有一个风格文件**（见「风格预设」节），各带各的锁定串。
它们是**并列的两种风格，不是同一风格的两个版本**：资产上的 `_3DCG版` / `_古风CG版` 是**风格标记**，
`_v2` / `_v3` 才是版本号（且只用于同风格内的重生成）。

**做法**：

| 层      | 处理                                                                       |
| ------ | ------------------------------------------------------------------------ |
| 角色生成器  | **一风格一脚本**：`gen_char_cg.py`（古风CG）/ `gen_char_3dcg.py`（国漫3DCG） |
| 角色产物   | 文件名带风格标记：`CH01_四视图_3DCG版.png`                                            |
| 关键帧生成器 | **同一脚本 +** **`--style`** **参数**，不复制脚本                                    |
| 关键帧产物  | 一风格一子目录：`图片/ref3d/`，避免互相覆盖                                               |
| 角色参考图  | 角色表里每风格一个字段（`file` / `file_ref3d`），`char_file()` 按当前风格取                  |
| `overlay` 资产 | 单独命名 + 单独子目录：`CH##_四视图_Q版.png`、关键帧进 `图片/qban/`（R37）                   |

**🔴 关键纪律：提示词的风格层与投喂的角色参考图必须同风格。**
换风格时若只改提示词、参考图还用旧的，模型会被旧图带偏，成图仍是旧画风
（实测踩过：切 3DCG 风格后成图仍是厚涂感，因为投喂的还是古风CG版四视图）。

**画风口径以"成片截图反推"优先于"提示词文本归纳"** 。
截图证明了这条管线**实际能产出什么**，而文本只是意图。

### 🔴 两种「并存」要分清（`main` 与 `overlay`）

| 类型 | 例子 | 关系 |
| --- | --- | --- |
| **历史并存（留档对照）** | 古风CG vs 3DCG（**都是 `main`**） | **同一时间只用一个**；另一套只是留档，供对照与回退 |
| **同时共存（叠加插播）** | 主画风 + Q版整活（**`main` + `overlay`**） | **同一时间两个都在用**；`overlay` 只覆盖它自己的那些镜头 |

> **判断法**：资产是"二选一"还是"两个都要投喂"？前者是历史并存，后者是叠加共存。
>
> 🔴 **叠加共存时，两套资产必须严格分区投喂**：主画风镜投 `_3DCG版`，整活镜投 `_Q版`。
> 混投的结果是模型拿两套矛盾的参考图去画，必崩（R37 / R27a）。

***

## 🔴 红线（任一违反都算失败）

**台词与节奏**

| #       | 规则                                                                                                                                                                                                            |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R1      | **现有台词不改**（保留原文，措辞基本不动）。改编只为让对话更贴合视频逻辑、更自然（调整顺序、补衔接词），**很多时候不用改**。禁止删原文信息/改人物立场/凭空发明剧情。**心理描写转表演动作**；纯叙述性画外音尽量少用                                                                                              |
| R2      | 台词用 `<d>[Chinese] 对话内容</d>` 包裹；`<d>` 就是边界。**不要加「」**；内容里可以有英文双引号，但标签外不要套括号                                                                                                                                     |
| **R2a** | 🔴🔴 **提示词中禁用双破折号** **`——`**（**仅限提示词**，说明性文档不受限，见上方范围说明）。`——` 是"连接/解释"符号，模型会把它**后面的字一起念进台词**（实测：U04 因此念出了剧本没有的半句话）。提示词里一律降级为句号/逗号。**时间码的单** **`—`（`0—2.2秒：`）不禁用**，那是格式骨架。详见 `references/prompt-format.md` §2b |
| **R2b** | 🔴 **注入到提示词里的固定文本，自己也要遵守提示词的全部规则**。曾把边界声明写成含 `——` 的句子，导致每提交一个单元就塞进 2 个破折号、8 个单元全污染。**加任何固定前缀/后缀前先问"这段自己合规吗"**，并加 `assert` 自检                                                                                  |
| **R2c** | **清理动作放在所有注入之后**。曾因清理早于注入，注入进来的破折号逃过了清理。提交链路的正确顺序：`风格层 → 注入 → 清理 → 提交前断言`                                                                                                                                     |
| R3      | **每句完整台词只写进它所属的那一个秒级段落**，分段切的是动作节点，不是句子。一句话拆两段会导致接缝停顿/重复起口型/吞字                                                                                                                                                |
| R3a     | 台词内容**不得在** **`<d>`** **之外复述**（「说到『很远很远』时」→「说到半句时」）                                                                                                                                                           |
| R4      | 台词时长 = **含标点字数 ÷ 4.5**（标点是停顿，也占时间；纯汉字口径会低估）                                                                                                                                                                   |
| R5      | 大分镜 **4-15 秒**；**超 15 秒必须拆成独立大分镜**，不是拆生成单元，是拆大分镜；编号连续，**禁止 B05a/B05b 半编号**                                                                                                                                    |
| R6      | 🔴 **画幅决定节奏档**。**16:9 默认档**：无台词大分镜 ≤**10 秒**；小分镜 1.5-6 秒；单集 ≤90 秒（**16:9 项目可显式放宽到 120 秒**，须在项目规范声明）。**9:16 收紧档**：无台词大分镜 ≤8 秒；单集 ≤90 秒。特殊无台词镜另可豁免，见 R6a                                                                                                                  |
| **R6a** | 🔴 **无台词段的本档上限可豁免，但有条件**。① 仅限两类：**铺垫镜**（为后续爆点蓄势，需一次把人物状态/空间关系/悬念铺足）与**长镜头**（一镜到底，靠运镜与调度承载冲击，即"爆款长镜头"）；② 仍受 **R5 的 15 秒硬上限**约束（服务端技术限制，超了会被截断，不是节奏偏好）；③ 必须在分镜表里**显式标注** **`[豁免:铺垫]`** **/** **`[豁免:长镜]`**，**不标注一律按当前画幅档卡**（16:9 → 10 秒 / 9:16 → 8 秒）；④ 每集建议 **≤2 个**、累计豁免时长 ≤单集 **20%**（项目规范可覆盖），因为 R7a 台词占比 ≥55% 仍要成立；⑤ **豁免只放松时长**，「不允许纯过场」「开场必须入戏」两条**不变**，豁免镜内部仍须有变化（运镜推进 / 表演层次 / 光影与声音推进），**静止凝视不算长镜头** |
| R7      | 时间码全片相对计时，大分镜区间 = 内部小分镜区间的并集，各级首尾相接                                                                                                                                                                           |
| **R7a** | **台词占比 ≥55%**（台词总时长 ÷ 单集时长）。**靠砍无台词剧情达成，不是加台词**。反推：`允许总时长 ≤ 台词时长 ÷ 0.55`                                                                                                                                      |
| **R7b** | **节奏不可拖沓**（松紧随画幅档变）：**16:9 默认档** 5 秒内必须入戏、无台词镜 3-4 秒、情绪镜可到 6 秒；**9:16 收紧档** 前 3 秒必须有冲突/异常/对话、无台词镜 ≤2.5 秒。两档通用：每个大分镜必须推动剧情，**不允许纯过场**；豁免镜见 R6a                                                                                                                                                    |
| **R7c** | **人物情绪要够足**：每段情绪有起点与终点；心理外化成动作；关键情绪点给足时间；禁止全程一个表情                                                                                                                                                             |

**改编授权（R1 的边界）**

| 项        | 规则                          |
| -------- | --------------------------- |
| **现有台词** | **不改**，保留原文，措辞基本不动          |
| **主要手段** | **压缩/删除无台词的剧情段**，让对话占比自然上升  |
| **改编目的** | 让对话更符合视频逻辑、更自然；**很多时候不需要改** |
| **可选增写** | 仅对话衔接生硬时补极少量，**不作为主要手段**    |
| 禁止       | 删除原文信息、改变人物设定或立场、凭空发明剧情事件   |

**传播与合规（传播层，规范见 `references/viral.md`）**

| #        | 规则                                                                                                                                                                                                                          |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **R33**  | 🔴 **选题必须过体检再开工**：六项爆款基因 ≥8/12；其中**「可视化冲突」「台词密度」任一为 0 分直接否决**（一个是没画面、一个是没台词，都做不成短剧）。凭感觉选品 = 用几十小时算力试错                                                                                                                 |
| **R34**  | 🔴 **钩子与包装必须显式标注，不许"分镜写完就提交"**：① **开场钩**：16:9 档 5 秒内、9:16 档 3 秒内，标 `[钩子:冲突]` / `[钩子:悬念]` / `[钩子:反差]`；② 末 5-10 秒标 `[结尾钩:悬念]` / `[结尾钩:危机]` / `[结尾钩:反转]`，力度须大于全集冲突；③ 情绪节点标 `[节点]`，相邻间隔 ≤30 秒；④ 包装三件套（标题 ≥3 候选 + 封面帧 + 简介）**并入 `剧本分镜.md` 第一部分**，**不新增 md 文件**（守住「只留 4 个 md」） |
| **R35**  | 🔴 **合规是一票否决闸门，不是注意事项**：私刑复仇／以暴制暴、迷信成真、未成年成人化、拜金炫富、婚恋对立、流血与刀具特写、标题极端引流词（灭门／逆天改命／疯批绝杀／绿帽／极致复仇）等，命中即整部驳回。**反派结局必须走法律程序**，诡异现象必须科学闭环                                     |
| **R36**  | 🔴 **AI 内容标注三处必留位**：AI 生成占比 >50% 须在 **片头前 3 秒 + 封面 + 简介**三处标注。**U01 必须在切分镜阶段就留出标注位**，不能后期补（后期补会挤掉开场钩）；封面与简介两处不入生成提示词。另：**单集不得低于 30 秒**（合规硬下限）                                            |
| **R37**  | 🔴 **Q 版整活镜（`overlay` 类画风，`styles/chibi-gag.md`）**：① **必须独立成大分镜 / 生成单元**，一套风格串只服务一个单元，**禁止与主画风镜头混写在同一提示词里**（R27a）；② 只投喂 **Q 版参考图**，不与主画风参考图混投；③ **画面内不生成文字**，科普与梗的解释走后期字幕；④ **只替代「画面尺度」类风险**（血腥／伤害／刀具／危险动作），**价值观类一票否决项不能靠 Q 版包装**（私刑复仇、迷信成真、未成年成人化等必须改剧情）；⑤ 触发处必须显式标注 `[整活:科普]` / `[整活:风险]` / `[整活:心理]`，**风险类还要写明替代了什么** |

**提示词与资产**

| #        | 规则                                                                                                                                                                                                                                                                                                           |
| -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| R8       | 参考图引用格式 `（@图片N）`，**全角括号、前后无空格**。**必须同时引用两类**：角色四视图（锁形象）+ **场景参考图**（锁环境）。`视觉风格` 段要复引                                                                                                                                                                                                                          |
| R8a      | **关键帧必须参考，但不强制复刻**。有分镜关键帧时必须投喂（I2VA 首帧参考），用于稳定画面基调、空间关系与人物状态；写「依（@图片N）关键帧为参考，保持人物、场景与画面基调一致」，**不要**写「构图完全不变」「画面与之一致」这类锁死表述，调度仍由提示词逻辑描述决定                                                                                                                                                                     |
| R8b      | **逻辑描述优先**：每段写清人物动机 / 情绪起点终点 / 空间关系 / 事件因果 / 心理外化。只写动作骨架会生成平淡                                                                                                                                                                                                                                                |
| **R8c**  | 🔴 **图像生成提示词必须符合逻辑与一致性**。①逻辑：物理/空间/时间/道具/世界观自洽，禁止自相矛盾（水中扬起干土、黄昏变正午）；②一致性：同一角色**逐字复用**外貌锁定常量、同一场景复用同一套环境词、每条都带画风尾串，禁止即兴改写措辞。详见 `references/character-consistency.md`                                                                                                                                          |
| R9       | 服务端图片上限 **7 张**（`NODE_IMAGES` 7 个节点，超出返回 400「最多支持 7 张」）；项目可在自身规范里定更严约定。典型配额：关键帧 1（必备参考）+ 角色四视图 1-3 + 场景参考图 1-2                                                                                                                                                                                               |
| R10      | 提示词 ≤1900 字（硬上限 2000）。`<d>` 标签每条约 +14 字，长单元预留 20-30 字                                                                                                                                                                                                                                                        |
| R11      | 🔴 **提示词正文一律用英文写作**（2026-09-18 用户拍板，推翻旧的中文口径）；台词原文保留不译。格式契约不变：台词仍 `<d>[Chinese] …</d>`、引用仍 `（@图片N）`、元数据表仍用中文表头。**只限视频提示词**，图像提示词（关键帧 / 四视图 / 场景图）保持中文不动。详见 `references/prompt-format.md` §6                                                                                                                  |
| **R11a** | 🔴 **只有音效，没有背景音乐**。不写 `non_diegetic_music` 段、不写乐器/旋律/氛围音乐；声音段末尾必须显式声明「无配乐」。🔴 **2026-09-20 定案：BGM 由用户后期自行添加，不属本链路职责，生成侧永不配乐**。因此写音效时要**给后期 BGM 留空间**：不要持续满铺、不要占满低频与中频（否则叠上 BGM 会糊成一团），台词所在的中高频段留给台词                                                                                                                                                                                                                                   |
| **R11b** | **音效要够细节**：写材质（木/土/布/铁）、距离与空间（远处户外混响 / 贴近室内闷响）、力度层次、混响干湿、声道布局。禁用抽象词（氛围感/情绪张力）                                                                                                                                                                                                                                |
| **R26**  | 🔴 **自动注入的固定文本会挤占字数预算**（R10）。它是**纯开销**，必须压到 \~100 字。实测：边界声明初版 203 字，把 U01 从 1588 顶到 **1911 字、越过 1900 上限**。**给提示词加任何固定前缀/后缀时都要同时算字数**，并在脚本里加闸门（超 `PROMPT_LIMIT` 告警、超 `PROMPT_HARD` 跳过并报错）                                                                                                                     |
| **R27**  | 🔴 **风格文本的权威来源是 `styles/<style_key>.md`**（见「风格预设」节），脚本里的规则只是它的**兜底实现**。**提示词与风格层分离**：风格层不写死在脚本里，而是**运行时**由 `apply_style_layer()` 处理；**不要为切风格复制提示词文档**（长文档复制两份会立刻失去同步）。现行做法是**文档本身已按目标口径写成**，此时替换函数退化为**兜底**（对同口径正文幂等：正则不命中、收敛项不重复追加）；仅当文档仍是旧口径时才真正做替换。替换规则用**正则**而非逐字匹配（同一短语在各单元有变体），替换后跑**残留自检**                                                                                |
| **R27a** | 🔴 **提示词的风格层与投喂的参考图必须同风格**。换风格时若只改提示词、参考图还用旧的，模型会被旧图带偏，成图仍是旧画风（实测：切 3DCG 风格后成图仍是厚涂感）。角色图按**风格**分文件（`file` / `file_ref3d`），由 `char_file()` 按当前风格取                                                                                                                                                                 |
| **R28**  | 🔴 **文字里出现谁，就必须明确表态**：要么进 `chars`（画面内、投喂四视图），要么进 `off_screen`（画面外、明确禁止画入）。**不许含糊略过**。反例：`shot` 写「画面深处琴案前坐着赵筱月」但 `chars` 只登记了另两人，导致 CH02 四视图未投喂、画出来不是本人。`off_screen` 是**双保险**：即使文字没提到某人，只要场景暗示他在场（屋内本该有琴案），也显式排除，防模型自行补人                                                                                      |
| **R28a** | **自检只认显式声明，不靠文字上下文猜**。判断"某人是否该投喂"时，不要用"画面之外/看不到"这类词去猜，靠猜会产生大量假阳性，噪音一多就会被习惯性忽略，自检也就废了                                                                                                                                                                                                                         |
| **R32**  | 🔴 **英文是视频提示词的正体**（2026-09-18 拍板，见 R11）。换语言要**连带改三处**，漏一处就出哑巴问题：① **字数闸门换制**（中文按字符 1900 / 英文按词 1100，换算约 1.7 字/词）；② **跳过中文风格层替换**，中文正则命不中英文文本，硬跑只会给英文提示词误追加一段中文「收敛项」；③ **自检换词表**，中文 A / B 类词表跨语言套用会全量误报或漏报，应显式跳过并标注「未覆盖」，**不要假装通过**。解析契约与语言无关，**不要动** **`parse_units()`**。详见 `references/prompt-format.md` §6 |

**提交与文件**

| #        | 规则                                                                                                                                                                                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| R12      | `duration` 只收**整数秒**，小数进位（6.5→7、11.5→12），宁可多不可少                                                                                                                                                                                                                                                                 |
| R13      | **rh-send 服务由用户启动**。只探测 + 提示，不代劳；重复启动会抢端口                                                                                                                                                                                                                                                                       |
| R14      | 🔴 **上游状态不可信**：`FAILED` 也可能落盘成功。判据是**磁盘文件**，且必须叠加**时间戳校验**：文件 `mtime` ≥ 提交时刻。只看"文件存在"会命中**历史同名旧文件**（服务端 `find_saved_video()` 遍历所有日期目录，返回第一个匹配），实测曾因此把昨天的成片当成"0 秒落盘成功"归档。🔴 **推论（2026-09-21 实测，必须照做）：落盘判据的第一顺位是「直接查 `save/<今天>/<task_id>.*`」，不能只读 `/task_query` 的 `saved` 字段。** 同一 task_id 在历史日期跑过时，`saved` 会**一直**指向旧日期的文件，于是新成片明明已经躺在 `save/<今天>/` 里，脚本却判定「未落盘」而空转到超时（实测白等 9 分钟，三条成片早在提交后 4 分钟就已落盘）。正确优先序：① `save/<今天>/<task_id>.*` + mtime → ② `saved` + mtime → ③ 从 `raw` 挖直链补调 `/save_video` |                                                                                                                                         |
| **R14a** | 🔴 **提交必须校验响应体**，不能只看 HTTP 状态码。上游出错时服务端**透传上游原始响应**、HTTP 仍 200。判据：响应里 `ok: true` **且** `upstream_task_id` 非空。`code 605` 的 msg 写 `NOT_ENOUGH_BALANCE` 但**不是余额问题**，真因是**并发超限**（见 R24）                                                                                                                             |
| **R14b** | **`task_query`** **返回 404 要立即中止**，不要重试。那是"提交根本没成功"的信号（并发超限/参数被拒），空转轮询 30 分钟毫无意义                                                                                                                                                                                                                                 |
| **R14c** | 🔴 **`save/<今天>/` 里有同名文件 ≠ 它是你的成片**：归档前先看 `mtime`，再看**时长是否对得上本次提交的 `duration`**。直接解析 mp4 的 `mvhd` box 即可读时长（`data.find(b'mvhd')` → version 0 取 `>II`，version 1 取 `>IQ`），**不必装 ffmpeg**。实测同一 task_id 在 09-16 与 09-21 各有一份，靠时长（5.49/6.08/7.85 秒 vs 7.29/7.29/9.42 秒）才确认没拿错。**实测成片时长恒比提交的 `duration` 长 0.2-0.5 秒**，属正常浮动 |
| R15      | **禁止删除任何已生成文件**。重生成加后缀：task\_id 加 `b/c`，文件加 `_v2/_v3`；`EP01U01b` ↔ `_v2` 一一对应                                                                                                                                                                                                                                   |
| R16      | 成功后**立即复制归档**到 `EP##/视频/`，否则项目目录看起来是空的                                                                                                                                                                                                                                                                          |
| R17      | HTTP\_PROXY 会劫持 localhost → 请求本地服务必须 `ProxyHandler({})` 禁用代理，否则 502                                                                                                                                                                                                                                             |
| **R21**  | **画幅先定，后动图**。🔴 **默认 16:9 横屏**（用户 2026-09-20 定）；**若改 9:16 竖屏**，则须同步改：关键帧与场景图的尺寸（`1680x944` → `1024x1536`）、提示词开篇的「16:9/9:16」声明、`aspect_ratio` 传值、**节奏收紧档**（见 `rhythm.md`）。🔴 **角色四视图不在此列**：它**固定 16:9（`1680x944`）与画幅无关**，改画幅**不用重出**。**旧画幅的关键帧/场景图全部重出，不要混用**                                                                                                              |
| **R21a** | 🔴 **`aspect_ratio`** **必须传上游完整枚举**（`16:9 (Widescreen)` / `9:16 (Portrait Widescreen)` …），**不要传裸冒号形式** **`16:9`**。服务端虽有转译函数，但**运行中的服务进程可能落后于源码**，裸值会被原样透传，上游 `ResolutionSelector` 报 `'16:9' not in [...]`，任务 **0 秒即 FAILED**。传完整枚举在新旧两版服务端下都正确                                                                  |
| **R24**  | 🔴 **视频并发上限约 5 个**，超了返回 `code 605`。用**滑动窗口**调度：**最多 5 个在飞，任一落盘立即补下一个**。**不要分批**（每批等最慢的会空转），也不要一次全丢。实现即 `ThreadPoolExecutor(max_workers=5)` 一次性 `submit`。并发时日志加 `[U07]` 前缀 + 加锁。🔴 **推论：滑窗期间槽位是恒满的**，所以「某个单元落盘了」**不能**当作「可以补交新任务」的信号，补交必定 605。要补交**只能等整批结束**（判据：该批所有单元都已归档）。**最好把待补的单元并进同一次** **`ALL`**，从根上避免这件事 |
| **R24a** | 判断「整批结束」要用**归档目录**（`EP##/视频/` 里该批单元文件是否齐全），**不要去数** **`save/`** **的新文件**。`save/` 是全日期共享目录，里面本来就躺着历史成片（实测扫出 42 个同名模式的历史文件），并不可靠；归档目录是每个 `gen_video.py` 进程自己写的，语义干净                                                                                                                                               |
| **R25**  | **失败要分类处置，别一律重试**：① **传输类**（`IncompleteRead` 等在数据快传完时断连）→ **直接重试即成功**；② **内容审核 400** → **改写措辞**，同样的组合重试还是拒；③ **上游** **`FAILED`** **但文件已落盘** → 视为**成功**（R14。🔴 实测 `errorCode 805`「工作流运行失败」+ `failedReason.node_name` 报某节点 `TypeError` 时，文件照样**完整落盘且时长正常**，三条成片全中此坑，别慌着重跑）；④ **`code 605`** → 并发超限，等槽位（R24）；⑤ **404** → 提交没成功，别轮询（R14b）                                                                                        |

**行为纪律**

| #       | 规则                                                                                                                                                                 |
| ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **R18** | 🔴 **不自我验证**：生成完直接交付，让用户反馈。**不要用 Read 工具逐张打开生成图去"检查质量"**，用户明确要求过"以后都不再验证"。画面好坏由用户裁决，不要自己看、自己评、自己决定重画。批量的容错由脚本的 SUCCESS/FAILED 输出来兜（**只处理 FAILED，不评价 SUCCESS 的观感**） |
| R19     | **不试探性单跑**：要多少直接并行出多少（关键帧可 10 并行），不先跑一张看效果                                                                                                                         |
| R20     | 数据可疑时**先确认口径再动手**，不要改完再报告                                                                                                                                          |
| **R22** | **一轮只修确定性的错**。成图若踩中"客观错误"（瞳色/发色/形制与设定相悖、服装漏改、比例错、出现文字叠印），定点改常量或提示词，重出**一次**即交付；**不做多轮调优、不凭主观审美反复重画**。主观好恶一律交用户裁决                                                   |
| **R23** | **只有脚本报 FAILED 才处理**（如 400 审核拒绝、传输中断）。诊断看**提示词文本**，不看生成图。审核类拒绝优先往"减少负面词密度 / 去肢体强制与未成年组合 / 拆句"方向改                                                                   |
| **R29** | 🔴 **归因必须靠对照实验，别拿"看起来可疑的邻近因素"当结论**。实测教训：U04 念出额外台词，我连续两轮误判为"动机描写贴在引导语位置"，改了措辞与语序都没解决；用户改**一个标点**（`——` → 句号）就修好了。**改变单一变量、观察结果是否反转**，才是定因                           |
| **R30** | 🔴 **用户划定范围后不要擅自扩大**。要求"只重跑 U04"就只跑 U04。实测教训：擅自顺手重跑了 7 个单元，被用户叫停并在上游取消。每个约 10 分钟，白耗算力。**动手前确认范围**                                                                  |
| **R31** | **用户可能先给一版口径、再推翻**（如某镜的配角：先要"补投人设图保留"，后改"只有两人"）。**第一次改动前值得问；用户主动改口径时直接执行，不要再问**                                                                                    |

***

## 🔴 台词边界三层保证

`<d>[Chinese] …</d>` 只能圈住"**该念什么**"，**圈不住"不该念什么"**。
提示词里写规矩是**概率性**的，脚本检查是**确定性**的，**边界由脚本守**。

| 层 | 手段                                         | 落在哪                                      |
| - | ------------------------------------------ | ---------------------------------------- |
| 1 | 写法纪律：台词前只放"怎么说"，动机/心理移到台词**之后**；提示词中禁用双破折号 | `references/prompt-format.md` §2 / §2b   |
| 2 | **自动注入**「朗读边界」声明（插在第一个秒级段落**之前**）          | `gen_video.py` 的 `apply_read_boundary()` |
| 3 | **自检**：`check_dialogue.py`（退出码 0/1，可卡流程）   | 项目 `EP01/`                               |

细节与实现坑（声明里不能带字面标签示例、反向词要做语境过滤、注入物自身污染）见
`references/prompt-format.md` §2b「台词朗读边界与可懂度」。

***

***

## 环境与服务

### rh-send（视频生成）

- 服务：`http://127.0.0.1:8282`，代码在 `H:\_Project\Python\AITest\RH_Send`
- **由用户手动启动**（`python main.py`）；启动前先探测 `/docs`
- 关键接口（**实测核实，勿凭记忆写**）：
  - `POST /file_upload` — multipart 上传参考图，字段名 `file`；
    🔴 取返回的 **`data.fileName`** 填进 `images`（**不是** **`image_id`**，也不是 `download_url`）
  - `POST /gen_video` — 提交（**不是** **`/run`**）；`aspect_ratio` 传**完整枚举**如 `16:9 (Widescreen)`（见 R21a）
  - `POST /task_query` — 单次查询，不阻塞
  - `POST /save_video` — 落盘（**唯一会写出成片文件的接口**）
- 图片上限 = `NODE_IMAGES` 节点数（7 张），超出 400；图片按顺序 zip 到 nodeId 585/587/573/564/565/566/567
- **并发上限约 5 个视频任务**，超了返回 `code 605`（见 R24）
- 落盘位置：`H:\_Project\Python\AITest\RH_Send\save\<YYYY-MM-DD>\<task_id>.mp4`
- `task_map.json` 记录 `<本地task_id> -> <上游taskId>`；两者**无关，别混用**
- 详见 `references/service.md`

### gpt-image-2（图像生成）

- 走本地代理 `http://127.0.0.1:7897`（系统代理 6224 会 502，直连会 WinError 10060）
- 输出尺寸必须**被 16 整除**。🔴 **两类资产规则不同**：**角色四视图固定 16:9 → `1680x944`（与画幅无关）**；**关键帧/场景图与成片画幅一致**（16:9 → `1680x944` / 9:16 → `1024x1536`）。⚠️ `1536x1024` 是 **3:2**，不是 16:9
- 内容审核严格：儿童主体 + 病理形容词组合易触发 `content_policy_violation`，
  病态描写改到成人主体上，儿童只写中性词（瘦小）
- 批量生成用 `run_in_background` 单张逐个跑，可 10 并行；单张约 50-70 秒
- 🔴 **实测（2026-09-21）：网关忽略 `size` 参数，按固定分辨率出图。**
  对照实验：请求 `1680x944` / `1712x960` / `1024x576`，**返回全部是 `1672x941`**（比例恒 1.7768），
  且宽高**不被 16 整除**、高度是**奇数**。含义与处置：
  ① **16:9 项目不受影响**（与真 16:9 差 0.06%）；
  ② `size` 仍照规范传 `1680x944`（那是**请求侧**约束，换网关后口径不变），返回尺寸不达标是上游行为、不是传错；
  ③ ⚠️ **不要拿这些图直接当视频帧**：941 是奇数，ffmpeg `yuv420p` 会失败；本链路里它们只作 I2VA 参考图，安全；
  ④ 🔴 **该网关出不了竖版**：若项目要改 9:16，**先验证** `gpt-image-2.5-flare` / `gpt-image-2.5-sunburst`
  是否遵守 `size`，或改「16:9 出图 + 裁切」方案，**不要在未验证前就按 9:16 排节奏档**；
  ⑤ **不做后处理补偿**（不缩放/不裁切/不补边），机械核对只记实测尺寸

### 参考图生成的两条路（各有硬缺陷，必须知道）

| 路       | 工具                    | 能做什么                      | **硬缺陷**                         |
| ------- | --------------------- | ------------------------- | ------------------------------- |
| **文生图** | `/images/generations` | 出基准四视图、场景图                | **无法投喂参考图**，角色一致性只能靠文字常量兜，五官仍会漂 |
| **图生图** | `/images/edits`       | 从基准件派生装束/表情/机位变体，**锁得住脸** | 变体的**左格半身照容易漏改服装**（下装与靴看右侧三视图）              |

**用法定式**：

- **基准件走文生图**，做完即冻结；
- **一切变体走图生图**，源图永远是基准件（**不要拿变体去改变体**，每转一手累积漂移）。

### Windows 环境

- Bash 工具 PATH 已损坏（`dirname`/`ls`/`tail`/`grep`/`tr` 全部 not found），
  但 **`curl`** **与绝对路径的 python 仍可用**。所以 **日志一律不要走管道**：
  `python x.py 2>&1 | tail -c 1200` 会直接 exit 127，改为 `> file.log 2>&1` 再 Read
- **PowerShell 工具的输出可能被吞**（退出码 0 但 stdout 为空）→
  需要结果时先 `Set-Content -Encoding UTF8` 落盘，再用 Read 读
- 凭证在 **Windows 用户级**环境变量，bash 读不到。两条路：
  ① PowerShell `[Environment]::GetEnvironmentVariable('X','User')`（受上面吞输出限制，要落盘）
  ② **python 用** **`winreg`** **直接读** **`HKCU\Environment`**（写在生成脚本里最省事，推荐；
  实测比调 powershell 少两跳，且不受吞输出影响）
- 校验文件用 Glob/目录扫描，**不看任务状态输出**
- 🔴 **代理端口会变，别照抄历史值**：脚本里写死端口前先探测一次。
  实测遇到过环境变量 `HTTP_PROXY` 指向一个**已死的端口**，照读会 SSL 握手超时，
  看起来像"网关挂了"；真因是代理选错。**可用端口与死端口可能并存**，
  所以脚本要**显式指定**已验证的那个，**不要读** **`HTTP_PROXY`**

***

## 参考文件（按需加载）

| 文件                                    | 何时读                                                                                                                                  |
| ------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------ |
| `platforms/<platform_key>.md`         | **开工选平台时先读**。画幅与时长档 / 节律档 / 标题封面规则（含**禁用词**）/ 审核尺度 / 音频口径 / **数据口径分级**（🟢硬规则·🟡经验值·⚪待核验）                                          |
| `styles/<style_key>.md`               | **开工选画风时先读**。风格特征、中英锁定串、NEG、资产命名标记；写任何图像/视频提示词前都按它取串                                                                                  |
| `references/viral.md`                 | 传播层规范（做分镜前必读）。选题体检 / 母题骨架与秒级节拍表 / 钩子坐标与标注 / 标题封面 / 平台机制 / **合规一票否决全表** / **数据归因表**                                                  |
| `references/prompt-format.md`         | 写提示词前必读。§0 结构总览与十项要求 / §1 秒级段落 / §2 台词格式 / **§2b 朗读边界与可懂度**（破折号禁忌、机械保证、反向词）/ §3 参考图引用 / §4 三大段 / §5 自检清单 / **§6 视频提示词一律英文（R11/R32）** |
| `references/rhythm.md`                | 切分镜前必读，分镜规则、改编授权、台词占比、节奏与情绪要求                                                                                                        |
| `references/naming.md`                | 建目录/命名/写提示词结构时，**剧集目录「只留 4 个 md」的约定**、图片与视频命名、**提示词文档的解析契约（中英双语块结构、三合一图片提示词）**、gen 脚本约定                                              |
| `references/character-consistency.md` | 生成角色四视图 / **场景参考图** / 关键帧前，外貌锁定、角色登记纪律、审核规避                                                                                          |
| `references/service.md`               | 提交视频前，rh-send 接口、**并发调度**、轮询与落盘判据、故障排查、gpt-image-2 环境                                                                                |

***

## 常见坑速查

| 现象                                                    | 真因                                                       | 处理                                                                                                  |
| ----------------------------------------------------- | -------------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 本地服务请求 502                                            | HTTP\_PROXY 劫持 localhost                                 | `ProxyHandler({})` 禁用代理（R17）                                                                        |
| 提交后查无此任务                                              | 服务没起                                                     | 探测 `/docs`，提示用户启动（R13）                                                                              |
| `status=FAILED` 但想要结果                                 | 上游状态不可信                                                  | 看 `save/<日期>/<task_id>.mp4` 是否存在（R14）                                                               |
| 台词说不完                                                 | 用了纯汉字口径 / duration 取整向下                                  | 含标点 ÷ 4.5；整数进位（R4/R12）                                                                              |
| **铺垫不够 / 长镜头被切碎**                                     | 把本画幅档的无台词上限（16:9 → 10 秒）当成了铁律，硬压无台词段                      | 铺垫镜与长镜头**可豁免**，标注 `[豁免:铺垫]` / `[豁免:长镜]`，上限放宽到 15 秒（R6a）                                            |
| **做完了但没人看**                                       | 只跑了制作链路，传播层没做                                            | 选题体检 → 钩子坐标 → 包装三件套 → 平台档位（R33/R34，见 `references/viral.md`）                                        |
| **3 秒/5 秒数据差**                                  | 前 3 秒在铺垫（地理介绍、自述身世、氛围空镜）                                 | 换开场钩三类之一并**显式标注**（R34）                                                                        |
| **完播高但播放低**                                      | 是**点击率**问题，不是内容问题                                         | 改标题与封面首帧，**别去重做内容**（归因表见 `viral.md` §8.2）                                                          |
| **整剧驳回、无删改空间**                                   | 踩了一票否决红线（私刑复仇／迷信成真／未成年成人化…）                            | 过合规闸门 R35；**反派结局改走法律程序**，诡异现象改科学闭环                                                              |
| **U01 装不下、开场铺不开**                                | 没预留 AI 标注位                                                | **切分镜阶段就留出片头前 3 秒的标注位**（R36），不要后期补                                                          |
| **整活镜插进去画面很怪（四不像）**                              | 把 Q 版和主画风**混写进了同一个提示词**，或投喂图混了                               | 整活镜**独立成单元**，只投 Q 版图；一套提示词只服务一套风格（R37 / R27a）                                                  |
| **Q 版小人认不出是谁**                                   | 只改了比例，**配色或标志性配饰也一起改了**                                   | Q 版只"降级"不"重设计"：发色/瞳色/服饰配色/配饰必须与主线一致，出完与主画风基准件并排对照（`chibi-gag.md`）                             |
| **以为 Q 版能规避所有审核风险**                              | 整活只掩盖**画面**，掩盖不了**事情本身**                                 | 只有画面尺度类（血腥/刀具/危险动作）能用整活替代；**价值观红线必须改剧情**（R37 / `chibi-gag.md` §6.2）                              |
| **后期叠上 BGM 后整体发糊**                                | 生成时音效满铺，占满了低频与中频                                        | 音效改**断续点状**、低频克制、中高频让给台词（R11a / `prompt-format.md` §4）；**音效已烧进轨里，后期救不回来**                |
| 口型重复、接缝停顿                                             | 台词被跨段拆分                                                  | 整句写进一个秒级段落（R3）                                                                                      |
| 图片上传被拒 400                                            | 单元参考图 >7 张                                               | 减到 ≤7，保关键帧 + 新角色（R9）                                                                                |
| 图片传了但画面没参考上                                           | 错把 `image_id` / `download_url` 填进 `images`               | 必须填 `/file_upload` 的 `data.fileName`                                                                |
| 角色长相每集漂移                                              | 只写正面描述、没引四视图                                             | 外貌锁定常量 + 负面约束 + 双引用                                                                                 |
| **主角换套装就变脸**                                          | 用文生图重画，没用图生图                                             | 从基准件 image\_edit，锁面部（见 character-consistency.md）                                                    |
| **瞳色/发色生成了错值**                                        | 常量没写死，模型自行发挥                                             | 正面写死 + 负面再钉一次（"常识性默认"也要显式写）                                                                         |
| 成图右下角疑似有叠印                                            | 多为衣摆/鞋/投影落入该区域                                           | 放大目视确认，**不要跑后处理**                                                                                   |
| 画幅改了但画面比例不对                                           | 关键帧/场景图仍是旧画幅                                              | 关键帧与场景图**全部重出**，不要混用；**四视图不受影响、不用重出**（R21）                                                            |
| **拿 16:9 素材投竖屏平台，数据差**                                | 本项目**默认 16:9**，而平台场景是 9:16                               | 选平台时先核画幅；改画幅要同时改关键帧/场景图尺寸与**节奏档**（R21）                                                          |
| **把 `1536x1024` 当 16:9 用**                               | 它是 **3:2**，不是 16:9                                          | 16:9 统一用 **`1680x944`**（被 16 整除）；四视图固定用它，与成片画幅无关                                              |
| **要 9:16 竖图却拿不到，出图永远约 1672x941**                        | 🔴 **网关忽略 `size` 参数**，按固定分辨率出图（实测三档请求返回同一尺寸）              | 16:9 项目不用管；**要竖版先验 2.5 系模型是否遵守 `size`**，或改「16:9 出图 + 裁切」，别先按 9:16 排节奏档 |
| 重生成后找不到旧版                                             | 覆盖了                                                      | 永不覆盖，加后缀（R15）                                                                                       |
| **换了画风，成图仍是旧画风**                                      | 关键帧脚本仍引用**旧风格的角色参考图**                                    | 角色参考图按**风格**分文件（`file` / `file_ref3d`），提示词与投喂图必须同风格                                                       |
| **角色图/关键帧找不到文件**                                      | 资产目录被重整过（多了一层子目录）                                        | 定位走 **递归查找**（`rglob`），不要写死单层路径                                                                      |
| **`ALL`** **参数无效果**                                   | `startswith('U')` 把 `ALL` 也过滤掉了                          | 先单独判断 `'ALL' in raw`，再筛 `U##`                                                                       |
| **成图"不好看"被否决**                                        | 常量与原文描写冲突（如原文"绝美/冷艳"却写成"素净无妆"）                           | **先回原文核描写**，别凭审美猜；原文有"朱钗"就别写成"银钗"                                                                   |
| **画出来的人不是该角色**                                        | `shot` 里描述了某人，但 `chars` 没登记 → 四视图未投喂                     | 文字里出现谁就必须表态：进 `chars`（画面内）或 `off_screen`（画面外）；脚本自检会查                                                |
| **角色念出了没写的台词**                                        | 🔴 **双破折号** **`——`，它后面的字会被一起念进台词**（早期误判为"动机描写的位置"，标本认错了） | **提示词中**禁用 `——`，降级为句号/逗号（R2a）；脚本自动降级 + `check_dialogue.py` 卡高危项                                     |
| **台词听不清**                                             | 提示词里写了**反向词**（破音/破碎/含混/交代不清）                             | 情绪改用音量/语速/气息承载，并追加"每个字都要清楚"；自检脚本会抓                                                                  |
| **任务 0 秒就 FAILED**                                    | `aspect_ratio` 传了裸冒号形式，服务端未转译                            | 传完整枚举 `16:9 (Widescreen)`（R21a）                                                                     |
| **脚本一直报「未落盘」、空转到超时，其实文件早在 `save/<今天>/` 里** | 只信 `/task_query` 的 `saved` 字段，而它命中了**历史日期目录**的同名文件 | 落盘判据第一顺位改成**直接查 `save/<今天>/<task_id>.*`**（R14 推论）；归档前再用时长复核（R14c） |
| **提交看似成功但没有 upstream\_task\_id**                      | 上游报错被服务端透传，HTTP 仍 200                                    | 校验响应体 `ok` + `upstream_task_id`（R14a）                                                               |
| **0 秒就"落盘成功"，文件却是旧的**                                 | `find_saved_video` 扫所有日期目录，命中历史同名文件                      | 校验 `mtime` ≥ 提交时刻（R14）                                                                              |
| **提交返** **`code 605`** **/** **`NOT_ENOUGH_BALANCE`** | **不是余额！是并发超限（约 5 个）**                                    | 滑动窗口限流，不要充值（R24）                                                                                    |
| **提示词字数突然超限**                                         | 自动注入的固定文本占了预算                                            | 注入物压到 \~100 字 + 脚本加闸门（R26）                                                                          |
| **改了提示词措辞但问题没解决**                                     | 认错了病因（邻近因素 ≠ 真因）                                         | 做**对照实验**：变单一变量、看结果是否反转（R29）                                                                        |
| **`python x.py \| tail`** **报 command not found**     | Windows 下 Bash 的 PATH 损坏                                 | 日志写文件再读，**不走管道**（`> x.log 2>&1`）                                                                    |
| **PowerShell 退出码 0 却没有任何输出**                          | 该工具的 stdout 被环境吞掉                                        | `Set-Content` 落盘 + Read；或在 python 里用 `winreg` 读环境变量                                                 |
| **图像/视频脚本 SSL 握手超时，像是网关挂了**                           | 读了环境里的 `HTTP_PROXY`，而它指向**已死的端口**                        | 脚本**显式指定**已验证的代理端口，不读 `HTTP_PROXY`                                                                  |
| **要出整集成片但本机没有 ffmpeg**                                | PATH 里没装                                                 | `pip install imageio-ffmpeg --proxy <代理>`，用 `get_ffmpeg_exe()` 取二进制                                 |
| **关键帧角色长相跟四视图对不上**                                    | 关键帧是文生图，一致性只能靠文字常量，而常量被手抄走样                              | 关键帧生成器**直接 import** 角色生成器的 `LOOK` / `NEG` 常量与场景生成器的 `SCENES`，**用机制保证逐字一致**，不靠人记                     |
| **素材映射表解析丢图（明明写了 3 张只认出 2 张）**                        | 用贪婪正则跨界回溯匹配表格行                                           | 改成按 `\|` 分列：`line.strip('\|').split('\|')`，再逐列取值                                                    |
| **`--list`** **显示的参考图数与文档不符**                         | 同上，解析器的问题而非文档的问题                                         | 先跑 `--list` 核对单元数 / 时长 / 图片数 / 词数四项，**通过了再提交**                                                      |
| **滑窗跑着的时候补交第 6 个任务，返 605**                            | 🔴 **滑窗任一落盘会立即补位，5 个槽位是恒满的**，「检测到有新落盘」**不等于**「槽位空了」      | 补交要等**整批结束**，判据是「**该批所有单元都已归档**」（数 `EP##/视频/` 里的文件），不是「save/ 里出现了新文件」。等归档齐了再 sleep 20s 给上游释放余量，然后提交 |
| **分开跑时最后交的那个单元老失败**                                   | 它被安排在主批次之后，而补交判据用错了                                      | 干脆把它并进同一次 `ALL` 提交；若因资产未就绪必须分开，就按上一行的「整批结束」判据等                                                      |

